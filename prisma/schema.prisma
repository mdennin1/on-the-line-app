// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// model Address {
//   id  String  @id @default(uuid())
//   city  String
//   country String @default("United States")
//   street  String
//   type  String
//   user User @relation("address", fields: [userId], references: [id])
//   userId String @unique
//   zip String
// }

// model Product {
//   id String @id @default(uuid())
//   name String
//   description String?
//   sku String
//   categoryRef Category @relation("category", fields:[category], references: [name])
//   category String
// }

// model Order {
//   id  String  @id @default(uuid())
//   user User @relation("orders", fields: [orderedBy], references: [id])
//   orderNumber Int @default(autoincrement())
//   products  Product[] @relation("items")
// }

model User {
  id  String   @id @default(uuid())
  // address Address? @relation("address")
  // cartItems Product[] @relation("cartItems")
  city  String
  country String  @default("United States")
  createdAt DateTime? @default(now())
  email    String
  firstName String?
  lastLogin DateTime? @default(now())
  lastName  String?
  // orders Order[] @relation("orders")
  password String
  phone   String
  street  String
  updatedAt DateTime? @updatedAt
  username String   @unique
  zip String
}

